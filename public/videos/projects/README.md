Demo videos for project pages.

Naming: <project-id>.mp4 (matches the id in src/data/index.ts), e.g. risc-v-cpu.mp4

Format: MP4 (H.264 + AAC) for broad browser support. Keep them web-sized —
a few MB, 1080p or less. Add a poster image alongside if you want a still
frame before playback.
